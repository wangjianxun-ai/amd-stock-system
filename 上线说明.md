# AMD 股票系统上线说明

推荐使用 GitHub Pages，免费、稳定，手机可以直接访问。

## 你需要准备

- 一个 GitHub 账号
- 你的 Finnhub API key

## 操作步骤

1. 打开 GitHub，新建仓库，例如 `amd-stock-system`。
2. 上传 `amd_stock_system_publish` 文件夹里的全部内容。
3. 打开仓库设置，进入 `Secrets and variables` -> `Actions`。
4. 新增 secret：
   - 名称：`FINNHUB_API_KEY`
   - 值：你的 Finnhub key
5. 进入 `Settings` -> `Pages`。
6. Source 选择 `GitHub Actions`。
7. 进入 `Actions`，运行 `Update AMD dashboard and publish`。
8. 成功后 GitHub 会给你一个网址。

以后手机打开这个网址，就能随时查看 AMD 股票跟踪页面。

## 自动更新规则

系统会在美股交易日自动更新行情数据。中文新闻默认不会被英文 API 内容覆盖。

